# Python Cheat Sheet Retro TV

Интерактивная шпаргалка по Python для начинающих в стиле ретро‑CRT телевизора.  
Проект помогает быстро освежать синтаксис Python в визуально приятном формате «старого монитора» с шумом, скан‑линиями и плавной прокруткой текста.

---

### 💖 Поддержать через ЮMoney
[Поддержать разработку](https://yoomoney.ru/to/4100117099095471)

---

## Демо

> 🔗 [Live‑демо:] (https://shutnik8.github.io/Python-Cheat-Sheet-Retro-TV/)

---

## Возможности

- Ретро‑TV интерфейс: экран старого CRT‑телевизора с эффектами шума, виньетки и «глитчей».
- Canvas + JavaScript: отрисовка текста шпаргалки и анимаций через `<canvas>` без фреймворков.
- Авто‑прокрутка текста с возможностью остановки (Pause / Auto Scroll).
- Кнопки выбора разделов: быстрый переход к нужной теме шпаргалки.
- Управление с клавиатуры: прокрутка стрелками и пробелом.

---

## Что внутри шпаргалки

Шпаргалка охватывает базовые темы Python для новичков:

- Переменные и типы данных (`int`, `float`, `str`, `bool`, `None`);
- Базовый ввод и вывод (`print`, `input`);
- Условные операторы (`if / elif / else`, тернарный оператор);
- Циклы `for` и `while`;
- Основные структуры данных: список, кортеж, словарь, множество;
- Функции и параметры по умолчанию;
- Работа со строками и полезные методы;
- Встроенные функции (`len`, `type`, `sum`, `max`, `min`, `abs`, `round` и др.);
- Импорт модулей (`import`, `from ... import`, псевдонимы `as`);
- Советы для новичков: отступы, стиль `snake_case`, работа с ошибками и `help()`.

---

## Технологии

- **HTML5** — разметка страницы и оболочка «телевизора».
- **CSS3** — ретро‑стилизация, градиенты, тени, оформление корпуса TV.
- **JavaScript (Vanilla)** — логика анимации CRT, скролла текста, переключения разделов.
- **Canvas API** — отрисовка содержимого экрана, шум, скан‑линии, глитч‑эффекты.

---

## Использование

1. Склонировать репозиторий:

```bash
git clone https://github.com/Shutnik8/Python-Cheat-Sheet-Retro-TV.git
cd Python-Cheat-Sheet-Retro-TV
